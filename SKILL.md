---
name: ticket-compare
description: "智能出行比价助手：输入出发地和目的地，自动查询火车票（12306）和机票，给出最划算的出行方案。触发词：查票、比价、出行、怎么去、最划算、机票还是高铁、订票。"
license: MIT
metadata:
  author: ticket-compare
  version: "1.3.0"
---

# 出行比价 Agent

自动查询 12306 火车票 + 携程机票，归一化比价，推荐最优出行方案。

## 前置条件

确认 Python 3 可用，以及 `scripts/` 目录下的脚本：

```
python3 "$SKILL_DIR/scripts/query_trains.py" --help
python3 "$SKILL_DIR/scripts/query_flights.py" --help
```

首次使用时会自动下载 12306 车站数据（缓存到 `scripts/stations.json`），约需 2-3 秒。

---

## 指令优先级

1. **`<MUST>`** — 必须执行，跳过会导致功能异常
2. **`<SHOULD>`** — 最佳实践，有理由可偏离
3. **`<NEVER>`** — 绝对禁止

---

## 场景路由

| 用户说（关键词） | 场景 | 跳转 |
|---|---|---|
| 查票/比价/出行/怎么去/最划算/机票还是高铁/帮我搜/看看火车票 | 搜索比价 | → 场景 A |
| 只看高铁/只看火车/只看机票/只要飞机/动车 | 单模式查询 | → 场景 B |
| 订票/买票/帮我下单/去12306/去携程买 | 引导下单 | → 场景 C |
| 转乘/中转/换乘/转车/没有直达/怎么转/经停 | 中转搜索 | → 场景 D |
| 推送/发到微信/微信小程序/推送卡片/发小程序/推送到微信 | 微信推送 | → 场景 E |

---

## 场景 A — 完整比价（核心）

<MUST>
> 必须同时查询火车和飞机，缺一不可
</MUST>

### A0. 获取当前北京时间（最高优先级）

<MUST>
> 每次查询前必须先获取当前北京时间，作为过滤已发车车次的基准
> 不要依赖 Python datetime.now()——系统时区可能不准确
</MUST>

使用 Python 获取 UTC+8 北京时间：

```bash
python3 -c "from datetime import datetime, timezone, timedelta; bj=datetime.now(timezone(timedelta(hours=8))); print(bj.strftime('%Y-%m-%d %H:%M'))"
```

保存为变量：
- `NOW_DATE`：当前北京日期（YYYY-MM-DD）
- `NOW_TIME`：当前北京时间（HH:MM）

### A1. 解析用户意图

从用户输入中提取：

| 参数 | 提取方式 | 默认值 |
|------|---------|--------|
| 出发地 | 城市名（如 "北京"） | **必须由用户提供** |
| 目的地 | 城市名（如 "上海"） | **必须由用户提供** |
| 到达截止时间 | "X点前到" / "X点到" / "12点需要到" | 无（不限制） |
| 出发日期偏好 | "明天"/"后天"/"周五"/"今天" | 明天 |
| 偏好 | "只看高铁" / "要最快的" / "500以内" | 无 |

<MUST>
> 如果用户给出了"X点前到Y地"的到达截止时间，必须反向计算合理的出发日期范围：
> 1. 先查用户偏好日期（如"明天"）出发的车次
> 2. 如果当天出发的车无法在截止时间前到达，自动向前扩展查询范围（如查"今天"傍晚出发的过夜车）
> 3. 如果当天出发的车可以到达但已过发车时间，标记为"已发车"并排除
</MUST>

### A2. 确定查询日期范围

根据到达截止时间 + 当前北京时间，决定查询哪些日期：

```
如果现在时间 >= 发车时间 → 排除该车次（已发车）
如果用户说"明天12点前到"：
  → 查明天出发的早班车/早班飞机（当天到达）
  → 也查今天傍晚出发的过夜车（次日早上到）
如果用户没有截止时间：
  → 只查用户指定的日期
```

如果缺少出发地或目的地，引导用户补充：

```
请告诉我：
- 出发城市：如"北京"
- 到达城市：如"上海"
- 出发日期（可选，默认明天）：如"5月15日"
```

### A3. 并行查询

<MUST>
> 根据 A2 确定的日期范围，可能需要查询多个日期。
> 例如用户"明天12点到北京"→ 同时查今天（过夜车）和明天（早班车+飞机）
</MUST>

同时执行脚本：

```bash
# 日期1（如今天 — 过夜车）
python3 "$SKILL_DIR/scripts/query_trains.py" "<出发城市>" "<到达城市>" "<日期1>" > /tmp/ticket_trains_1.json
# 日期2（如明天 — 当天到达的车）
python3 "$SKILL_DIR/scripts/query_trains.py" "<出发城市>" "<到达城市>" "<日期2>" > /tmp/ticket_trains_2.json
# 航班（查日期2）
python3 "$SKILL_DIR/scripts/query_flights.py" "<出发城市>" "<到达城市>" "<日期2>" > /tmp/ticket_flights.json
```

### A4. 过滤已发车车次

<MUST>
> 对于所有查询结果，使用 A0 获取的当前北京时间过滤：
> - 如果 `dep_time < NOW_TIME` 且 `date == NOW_DATE` → 标记为 "已发车"，排除出推荐列表
> - 但保留在"已错过"区供用户参考
</MUST>

### A5. 处理航班查询失败的兜底

如果 `query_flights.py` 返回了 `fallback` 字段（携程 API 不可用）：

<MUST>
> 使用 WebSearch 搜索 "{出发地} 到 {目的地} {日期} 机票"，从搜索结果中手动提取飞机航班信息
</MUST>

从 WebSearch 结果中解析以下信息：
- 航班号（如 CA1234）
- 出发/到达时间
- 经济舱最低价格
- 航空公司

将解析结果归一化为与携程返回相同的格式，继续后续流程。

### A6. 执行比价

```bash
python3 "$SKILL_DIR/scripts/compare.py" "$(cat /tmp/ticket_trains_merged.json)" "$(cat /tmp/ticket_flights.json)"
```

### A7. 输出格式

输出必须包含**时间紧迫度提示**：

```markdown
> ⏰ 当前北京时间：{NOW_DATE} {NOW_TIME}

## 🚀 {出发地} → {目的地} | {日期范围}

### 火车票（{count} 趟可用）

| 车次 | 出发 | 到达 | 历时 | 二等座 | 一等座 | 商务座 |
|------|------|------|------|--------|--------|--------|
| G1 | 09:00 | 13:30 | 4h30m | ¥553 | ¥933 | ¥1748 |

### 已错过（{count} 趟已发车）

> 如果存在已发车且能在截止前到达的车次，在此列出

| 车次 | 发车时间 | 状态 |
|------|---------|------|
| G101 | 07:00 | 🚫 已于 {N}小时前发车 |

### 机票（{count} 班）

| 航班号 | 航空 | 出发 | 到达 | 历时 | 经停 | 经济舱 |
|--------|------|------|------|------|------|--------|
| CA1234 | 国航 | 08:00 | 10:00 | 2h | 直飞 | ¥850 |

### 推荐方案

| 排名 | 方式 | 方案 | 费用 | 耗时 | 性价比 |
|------|------|------|------|------|--------|
| 1 | 🚄 | G1 二等座 | ¥553 | 4h30m | ⭐⭐⭐⭐⭐ |

> **推荐理由**：{recommendation_text}
```

### A8. 比价建议

根据结果给出客观分析：

- **价格优先**：推荐最低价方案
- **时间优先**：推荐最快方案
- **性价比**：综合评价格和时间，标注"多花 ¥X 省 Y 小时"的权衡
- **时间紧迫度**：如果有车次即将发车（2小时内），高亮提醒用户赶紧下单

---

## 场景 B — 单模式查询

只查询火车或飞机。

### B1. 只查火车

```bash
python3 "$SKILL_DIR/scripts/query_trains.py" "<出发城市>" "<到达城市>" "<日期>"
```

将 JSON 结果转为表格展示：

```markdown
## 🚄 {出发地} → {目的地} 火车票 | {日期}

| 车次 | 出发站 | 到达站 | 出发 | 到达 | 历时 | 席位/票价 |
|------|--------|--------|------|------|------|-----------|
| G123 | 北京南 | 上海虹桥 | 09:00 | 13:30 | 4h30m | 二等座 ¥553 / 一等座 ¥933 |
```

### B2. 只查飞机

```bash
python3 "$SKILL_DIR/scripts/query_flights.py" "<出发城市>" "<到达城市>" "<日期>"
```

```markdown
## ✈️ {出发地} → {目的地} 机票 | {日期}

| 航班号 | 航空 | 出发 | 到达 | 历时 | 经停 | 经济舱 |
|--------|------|------|------|------|------|--------|
| CA1234 | 国航 | 08:00 | 10:15 | 2h15m | 直飞 | ¥850 |
```

---

## 场景 C — 引导下单

<MUST>
> 本 skill 不直接对接购买流程，始终引导用户到官方渠道完成支付
</MUST>

输出下单指引：

```markdown
## 购买渠道

### 🚄 火车票 → [12306 官网](https://kyfw.12306.cn)
- 出发站：{dep_station}，车次：{train_code}
- 直接在 12306 搜索该车次即可购票

### ✈️ 机票 → [携程](https://flights.ctrip.com) / [飞猪](https://www.fliggy.com)
- 航班：{flight_code}
- 在 OTA 平台搜索该航班号购买

> 如需我帮你生成直达链接，请提供具体的车次/航班号。
```

---

## 场景 D — 中转搜索（无直达或用户要求转乘）

<MUST>
> 当 A→B 无直达车次/航班，或直达方案无法满足到达截止时间时，自动搜索中转方案
> 最多支持 2 次中转（即 A→X→Y→B 三段行程）
> 每次中转必须考虑换乘时间（火车≥30分钟，飞机+火车≥2小时，飞机+飞机≥1.5小时）
</MUST>

### D1. 确定中转枢纽城市

根据 A 和 B 的地理位置，选择合理的中转枢纽：

| 方向 | 自然枢纽 |
|------|---------|
| 南方→东北 | 长沙、武汉、郑州、北京、沈阳、哈尔滨 |
| 南方→西北 | 西安、兰州 |
| 南方→西南 | 贵阳、昆明、重庆 |
| 东部→西部 | 郑州、西安 |
| 全国通用 | 北京、郑州、武汉、长沙、西安 |

<MUST>
> 优先选择地理位置在 A 和 B 之间、且有高铁/航班网络的枢纽城市
> 不要选择绕路的枢纽（如从长沙去北京，不应在昆明转）
</MUST>

### D2. 搜索策略

```
第一步：搜索 A→B 直达（已有的场景 A/B）
第二步：如果没有直达或直达不满足截止时间：
  → 1次中转：搜索 A→Hub→B
    选择 2-3 个最合理的枢纽城市
    并行查询 A→Hub 和 Hub→B
    筛选换乘时间合理的组合
  → 2次中转：搜索 A→Hub1→Hub2→B（仅当1次中转无解时）
    选择最有希望的枢纽组合
```

### D3. 换乘时间约束

| 换乘类型 | 最少换乘时间 | 说明 |
|----------|-------------|------|
| 火车→火车（同站） | 30 分钟 | 考虑出站+进站 |
| 火车→火车（异站） | 2 小时 | 如北京西→北京站 |
| 飞机→火车 | 2 小时 | 机场到火车站 |
| 火车→飞机 | 2.5 小时 | 火车站到机场+安检 |
| 飞机→飞机（同机场） | 1.5 小时 | 转机通道 |
| 飞机→飞机（异机场） | 4 小时 | 跨机场转运 |

### D4. 输出格式

```markdown
## 🔄 {出发地} → {目的地} 中转方案 | {日期}

> 无直达车次/航班，以下为中转方案

### 方案一：经 {hub} 中转（1次换乘）

| 段 | 方式 | 车次/航班 | 出发 | 到达 | 耗时 | 席位 | 价格 |
|----|------|-----------|------|------|------|------|------|
| ① {A}→{Hub} | 🚄 | G123 | 08:00 | 12:00 | 4h | 二等座 | ¥500 |
| ⟳ 换乘 | — | 在{Hub}等候 {X}h{Y}m | — | — | — | — | — |
| ② {Hub}→{B} | 🚂 | K456 | 14:00 | 20:00 | 6h | 硬卧 | ¥200 |

| 总耗时 | 总费用 | 换乘次数 |
|--------|--------|----------|
| {total}h | ¥{price} | 1 次 |

### 方案二：经 {hub1}、{hub2} 中转（2次换乘）

...类似格式...
```

### D5. 中转结果排序

- **换乘次数少 > 总耗时短 > 总费用低**（优先级递减）
- 如果用户有到达截止时间，先过滤后排序

---

## 场景 E — 微信小程序推送付款信息

<MUST>
> 用户确认出行方案后，将票务付款信息（含 12306/携程直达付款链接）推送到用户微信
> 推送前必须确认用户已选定具体方案（车次/航班号明确）
> 推送内容是「待付款」卡片，引导用户到 12306 或携程完成支付
</MUST>

### E1. 前置确认

推送前向用户确认：

```
确认推送付款信息到微信？

[12306] 待付款
  G89 北京西 → 长沙南
  日期：2026-05-13  出发：09:00 → 到达：14:30
  席位：二等座  ¥649
  付款截止：30分钟内

是否推送？
```

### E2. 执行推送

<MUST>
> 必须携带 `--secret-key`（12306 字段）、`--from-code` / `--to-code`（车站电报码）用于生成直达付款链接
> 机票必须携带 `--from-iata` / `--to-iata`（IATA 代码）生成携程直达链接
</MUST>

**火车票**（从 compare.py 归一化输出中提取 `secret_key`、`dep_station_name` 等字段）：

```bash
python3 "$SKILL_DIR/scripts/wechat_push.py" \
  --mode train \
  --train-code "G89" \
  --secret-key "abc123def456" \
  --from "北京西" \
  --to "长沙南" \
  --from-code "BJP" \
  --to-code "CSQ" \
  --date "2026-05-13" \
  --dep-time "09:00" \
  --arr-time "14:30" \
  --seat "二等座" \
  --price "649"
```

**机票**：

```bash
python3 "$SKILL_DIR/scripts/wechat_push.py" \
  --mode flight \
  --flight-code "CA1234" \
  --airline "中国国航" \
  --from "北京" \
  --to "上海" \
  --from-iata "BJS" \
  --to-iata "SHA" \
  --date "2026-05-13" \
  --dep-time "08:00" \
  --arr-time "10:15" \
  --seat "经济舱" \
  --price "850"
```

<MUST>
> 如果环境变量（WECHAT_APPID, WECHAT_SECRET, WECHAT_OPENID）未配置，脚本输出可分享的付款卡片文本
> 让用户复制后通过微信分享
</MUST>

### E3. 输出格式

推送成功时：

```markdown
## 付款信息已推送到微信

> 请在付款截止前登录 12306 / 携程完成支付

| 项目 | 详情 |
|------|------|
| 平台 | 12306 / 携程 |
| 车次/航班 | {train_code} / {flight_code} |
| 路线 | {from} → {to} |
| 出发 | {date} {dep_time} |
| 到达 | {date} {arr_time} |
| 席位/舱位 | {seat_class} |
| 票价 | ¥{price} |
| 付款截止 | {pay_deadline} |
| 付款链接 | [立即付款]({pay_url}) |
```

未配置微信时（兜底，输出可分享的付款卡片）：

```markdown
## 可分享的付款卡片

> 微信推送未配置（需要 WECHAT_APPID / WECHAT_SECRET / WECHAT_OPENID 环境变量）
> 复制以下付款卡片分享给微信好友：

========================================
  [12306] 待付款 — 出行票务
========================================

  {出发地} → {目的地}
  日期：{date}
  车次：[TRAIN] {train_code}
  出发：{dep_time}  →  到达：{arr_time}
  席位：{seat}
  票价：¥{price}

----------------------------------------
  付款截止：30分钟内
  请在30分钟内登录12306完成付款，超时订单自动取消
----------------------------------------

  立即付款：{pay_url}

========================================
```

### E4. 付款直达链接生成逻辑

`wechat_push.py` 现在为火车票生成 **3 种链接**（按优先级）：

| 类型 | 格式 | 说明 |
|------|------|------|
| 📱 微信小程序 URL Scheme | `weixin://dl/business/?appid={appid}&path={path}&query=from_station={code}&to_station={code}&date={date}` | **主链接** — 微信内点击直接跳转铁路12306小程序购票页 |
| 🌐 明文 URL Scheme | `https://wxaurl.cn/{appid}/{path}?from_station={code}&to_station={code}&date={date}` | 微信官方格式，内外均可唤起小程序 |
| 12306（浏览器） | `https://kyfw.12306.cn/otn/leftTicket/init?linktypeid=dc&fs={from_code}&ts={to_code}&date={date}&flag=N,N,Y` | 预填出发站、到达站、日期，用户登陆后直接进入预订页 |
| 12306（微信小程序） | `weixin://dl/business/?appid=wx3a455a4f1266a42a&path=pages/ticket/query/query&query=from_station={from_code}&to_station={to_code}&date={date}` | 微信内直接打开12306小程序，无需跳转浏览器。appid=wx3a455a4f1266a42a是12306官方小程序固定ID |

**铁路12306 微信小程序配置：**
- AppID: `wx3a455a4f1266a42a`（经公开资料确认）
- 购票页面路径: `pages/ticket/query/query`
- 查询参数: `from_station={站码}&to_station={站码}&date={YYYY-MM-DD}`

**代码参考（`wechat_push.py` 中）：**
```python
WECHAT_MP_12306_APPID = "wx3a455a4f1266a42a"
WECHAT_MP_12306_TICKET_PATH = "pages/ticket/query/query"
```

`build_12306_deep_link()` 返回 dict：
```python
{
    "wechat_scheme": "weixin://dl/business/?...",
    "wxaurl_scheme": "https://wxaurl.cn/...",
    "web_url": "https://kyfw.12306.cn/...",
}
```

付款卡片输出格式自动展示小程序链接和网页备用链接。

<SHOULD>
> 12306 付款时限为 30 分钟（从下单起算），卡片中必须标注
> 携程付款时限以页面为准，部分特价票可能仅保留 15-30 分钟
> 微信小程序推送需要用户在微信公众平台配置订阅消息模板
> 默认模板 ID 可通过环境变量 `WECHAT_TEMPLATE_ID` 覆盖
</SHOULD>

### E5. 微信订阅消息模板

推送使用微信订阅消息 API `POST /cgi-bin/message/subscribe/send`，模板字段映射：

```json
{
  "touser": "{openid}",
  "template_id": "{template_id}",
  "page": "{pay_url}",
  "data": {
    "thing1": {"value": "{出发地} → {目的地}"},
    "time2": {"value": "{date} {dep_time}"},
    "thing3": {"value": "{车次/航班号}"},
    "character_string4": {"value": "{席位} ¥{价格}"},
    "thing5": {"value": "请在付款截止前完成支付"}
  }
}
```

<NEVER>
> 不得在未确认方案的情况下自动推送
> 不得在推送内容中包含用户的敏感支付信息（银行卡号等）
> 本 skill 不直接处理支付，仅提供跳转链接
</NEVER>

---

## 输出原则

1. **时间标注首位**：输出开头必须显示当前北京时间
2. **表格优先**：用 Markdown 表格呈现比价结果，直观清晰
3. **价格突出**：所有价格用 `¥` 加粗
4. **推荐要有理由**：不只给排名，还要解释为什么
5. **处理无票情况**：如果某类票售罄，明确告知，不要假装有票
6. **诚实告知局限性**：携程 API 可能不稳定，此时主动说明并尝试 WebSearch 兜底
7. **不做代购**：本 skill 只做信息查询和比价，不直接参与交易
8. **紧迫度提醒**：如果有车次/航班即将发车（2小时以内），必须高亮提醒
9. **已错过标记**：已发车的车次放在单独区域，避免用户误判
10. **推送前确认**：推送微信前必须让用户确认方案，不得自动推送
11. **兜底卡片**：微信未配置时，始终输出可复制的分享卡片，确保用户可手动分享

---

## 时间感知规则（贯穿所有场景）

<MUST>
> 1. 每次查询前，必须先获取 UTC+8 北京时间
> 2. 用户提到到达截止时间时，反向推算合理的出发日期（可能跨天）
> 3. 过滤掉当前时间之后已发车的车次
> 4. 对截止时间的查询，优先展示能在截止前到达的方案
> 5. 将已错过的车次放在单独的"已错过"区域
</MUST>

| 用户说 | 时间约束 | 查询策略 |
|--------|---------|----------|
| "明天 12 点到北京" | 到达 ≤ 明天 12:00 | 查今天傍晚过夜车 + 明天早班车/飞机 |
| "今天下午去上海" | 出发 ≥ 现在 | 查今天剩余车次 |
| "后天上午出发" | 出发 ≈ 后天上午 | 查后天 06:00-12:00 发车的 |
| 无时间限制 | 无 | 查指定日期全部车次 |

---

## 示例：时间感知的完整交互

用户："我明天上午12点需要到北京"

1. **获取北京时间** → `2026-05-12 14:30`
2. **解析意图** → 出发地=当前城市（需询问），目的地=北京，截止=明天 12:00
3. **确定查询日期** → 查 5/12（今天傍晚过夜车）+ 5/13（明天早班车/飞机）
4. **过滤已发车** → 5/12 的 06:00-14:30 出发的车已过，14:30 之后出发的可用
5. **输出** → 先展示可用方案，再列出已错过的车次
